---
version: "1"

org: foo

services:
  - id: 1
    name: foo bar service
    platform: python
    team: foo
    settings:
      rate_limit: 1
    auth_keys:
      - key: key
      - key: expired_key
        expired_at: 2000-01-01T00:00:00.000Z
      - key: disabled_key
        disabled: true
teams:
  - id: 1
    name: foo
