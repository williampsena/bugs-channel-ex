---
version: "1"

org: foo

services:
  - id: 1
    name: foo bar service
    platform: python
    team: foo
    settings:
      rate_limit: 1
    auth_keys:
      - key: key
teams:
  - id: 1
    name: foo
